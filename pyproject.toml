[project]
name = "bigdata-ece"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "m-curie-ece", email = "maxime.curie@edu.ece.fr" }
]
requires-python = ">=3.13"
dependencies = [
    "faker>=40.39.0",
]

[project.scripts]
dataset-users = "bigdata_ece.dataset_users:main"
dataset-orders = "bigdata_ece.dataset_orders:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
